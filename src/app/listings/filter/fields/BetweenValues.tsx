interface BetweenValuesProps {
    min: number;
    max: number;
}

const BetweenValues = ({min, max}: BetweenValuesProps) => {
    return (
        <div className="between-values">

        </div>
    )
}

export default BetweenValues;